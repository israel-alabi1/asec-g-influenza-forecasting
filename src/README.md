# ASEC-G forecasting pipeline

The source directory contains the provisional ASEC-G forecasting engine and validation utilities.

The implementation is designed to operate only on the supplied MAEPiMS dataset. It does not fetch external epidemiological data or use network access.
